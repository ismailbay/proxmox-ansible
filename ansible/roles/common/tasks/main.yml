---
- name: Set timezone
  community.general.timezone:
    name: "{{ proxmox.timezone }}"
  notify: Reboot nodes

- name: Install base packages
  ansible.builtin.apt:
    name: "{{ proxmox.base_packages }}"
    state: present
    update_cache: "true"
